[tool.poetry]
name = "package-feed-relink-tool"
version = "0.1.0"
description = ""
authors = ["Jack Arnold <jack.arnold@ni.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
click = "^8.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
